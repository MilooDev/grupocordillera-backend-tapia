server:
  port: 8099  # <-- Lo unificamos a 8099 para evitar chocar con zombies

spring:
  application:
    name: gc_reportes_pdf
  
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/cordillera_db}
    username: cordillera_admin
    password: admin123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE:http://localhost:8761/eureka/}